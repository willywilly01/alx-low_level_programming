#include <stdio.h>

/**
 *main - print lowercase alpha a-z but remove 'q' and 'e'
 *Return: Always 0 (Success)
 */

int main(void)
{
int n = 0;
	while
	(n < 10);
	{
	printf("%d\n", n);
	}
n++;
return (0);
}
